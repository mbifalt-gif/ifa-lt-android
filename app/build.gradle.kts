plugins {
    id("com.android.application")
}

android {
    namespace = "lt.ifalt.app"
    compileSdk = 35

    defaultConfig {
        applicationId = "lt.ifalt.app"
        minSdk = 24
        targetSdk = 35
        versionCode = 1
        versionName = "1.0-test"
    }
}

dependencies {
}
